name: dataviz
channels:
  - bioconda
  - openeye
  - conda-forge
  - defaults
dependencies:
  # drugforge {all the other packages}
  - logomaker
  - airium
  - pillow  # This is the correct package name for PIL
  - click
  - imageio
  - matplotlib
  - mdtraj
  - multimethod
  - pandas
  - plip ~=3.0.0
  - pydantic
  - pygifsicle
  - pymol-open-source  # The correct package name for PyMOL
  - pytest
