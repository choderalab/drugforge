name: docking
channels:
  - openeye
  - conda-forge
dependencies:
  # Base depends
  - pip
  - pydantic >=2.0.0a0
  - pydantic-settings
  - typing-extensions
  - semver

  # drugforge-data
  # drugforge-modeling
  # drugforge-ml
  # drugforge-spectrum
  # drugforge-dataviz

  # Command Line Tools & Scripting
  - rich <14.1.0
  - click
  - pyyaml

  # Testing
  - pooch
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - pytest-console-scripts
  - pytest-timeout
  - requests-mock

  # Molecule Manipulation
  - biopython
  - mdanalysis
  - openeye-toolkits
  - openfe >=1.5.0
  - openff-toolkit
  - openmm
  - gufe >= 1.7.1
  - pymol-open-source
  - rdkit
  - plip ~=3.0.0

  # Math
  - numpy
  - pandas
  - sigfig

  # Execution
  - netifaces
  - dask
  - dask-jobqueue
  - distributed

  # Services
  - requests
  - moto
  - boto3

  # ML
  - mtenn >=0.6.3
  - multimethod
  - dgl<=2.0.0
