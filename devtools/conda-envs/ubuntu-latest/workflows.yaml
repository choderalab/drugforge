name: workflows
channels:
  - openeye
  - conda-forge
dependencies:
  # drugforge {all the other packages}
  click
  pydantic
  unittestname: workflows
channels:
  - openeye
  - conda-forge
dependencies:
# drugforge {all the other packages}
